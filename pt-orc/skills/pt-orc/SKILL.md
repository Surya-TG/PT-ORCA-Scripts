---
name: pt-orc
version: "0.73"
description: >
  [v0.73] PRIMARY PT SKILL — load this first and only this for any penetration testing
  engagement. Active dispatcher for all PT phases (recon, enum, evidence, report).
  Reconstructs full engagement context from state snapshot alone. Manages stage
  progression, return passes, and L2/L3 skill invocation. Trigger on: "pentest",
  "PT engagement", "PTI", "PTE", "findings", "recon", "scope", "nmap", "Metasploit",
  "PT report", "re-test", "db_nmap", "MSF workspace", "evidence", "save state",
  "state snapshot", or any engagement reference code (e.g. Acme-PTI-Mar-2026).
  REPLACES pt-workflow skill — do not load both.
---

<!-- L1 ORC-NAV — read MRK:NAV_TOC first; fetch MRK ranges precisely (no default line count) -->
<!-- L2 NAV:v1 → ../../../AUDIT-ORC-INDEX.md -->

<!-- MRK:SKILL_NAV_TOC — Section index | nav,toc,index | L18-37 -->
<!-- - MRK:PTORC_ROLE — Role | ptorc,role | L38-48 -->
<!-- - MRK:PTORC_PRINCIPLES — Core Principles (Immutable) | ptorc,principles,core,immutable | L49-61 -->
<!-- - MRK:PTORC_NAMING — Engagement Naming | ptorc,naming,engagement | L62-72 -->
<!-- - MRK:PTORC_DIRS — Directory Structure | ptorc,dirs,directory,structure | L73-136 -->
<!-- - MRK:PTORC_STAGES — Stage Registry | ptorc,stages,stage,registry | L137-156 -->
<!-- - MRK:PTORC_DISPATCH — Dispatch Table — L2 Skills | ptorc,dispatch,table,l2,skills | L157-178 -->
<!-- - MRK:PTORC_RETURNPASS — Return Pass Protocol | ptorc,returnpass,return,pass,protocol | L179-196 -->
<!-- - MRK:PTORC_STATE — State Snapshot — Schema | ptorc,state,snapshot,schema | L197-420 -->
<!-- - MRK:PTORC_SESSIONSTART — Session Start Protocol | ptorc,sessionstart,session,start,protocol | L421-427 -->
<!-- - MRK:PTORC_SESSIONEND — Session End Protocol | ptorc,sessionend,session,end,protocol | L428-436 -->
<!-- - MRK:PTORC_CHECKLISTS — Checklists | ptorc,checklists | L437-477 -->
<!-- - MRK:PTORC_PITFALLS — Common Pitfalls | ptorc,pitfalls,common | L478-506 -->
<!-- NAV-LEN: 12 entries | Integrity-hash: f5f9451fd04092ba | Last-indexed: 2026-06-09T07:09:41Z -->

# PT Orchestrator — pt-orc
*L1 — always loaded, never dropped during an engagement session*

---

## MRK:PTORC_ROLE — Role | ptorc,role | L38-48

This skill is the single entry point for all PT work. It:
1. Reconstructs engagement context from the state snapshot
2. Determines current position and next action
3. Dispatches the correct L2 skill for the requested work
4. Receives L2 output and updates the state snapshot
5. Manages return passes to earlier stages without losing progress

---

## MRK:PTORC_PRINCIPLES — Core Principles (Immutable) | ptorc,principles,core,immutable | L49-61

1. **Evidence-first.** Every finding traces to at least one captured artefact on disk. No finding is written without evidence.
2. **Three evidence streams:** MSF DB (queryable source of truth) · Automated raw logs (tool output under `evidence/`) · Manual analyst input (terminal sessions, curl, dig). All three maintained and correlated.
3. **MSF DB with caveats.** When DB conflicts with observed evidence, raw log wins. Flag with `notes -a -h <IP> -t db.caveat`.
4. **Validation pipeline:** `detected → suspected → validated → high_confidence`. Confidence maps: UNCERTAIN→suspected, LIKELY→validated, CERTAIN→high_confidence.
5. **Confirmed vs. precautionary.** Confirmed = exploited or impact demonstrated. Precautionary = banner/version matches CVE, not exploited. Mark `[Precautionary]`, confidence LIKELY.
6. **Finding vs. Observation.** Every F-XX requires a Recommendation. OBS-XX is informational only — no Recommendation ever.
7. **Versioning.** Every change increments version in filename AND internal field. Never reissue same version number.
8. **Evidence-based only.** No speculative content. No invented findings. CVE references where applicable.

---

## MRK:PTORC_NAMING — Engagement Naming | ptorc,naming,engagement | L62-72

```
<ORG>-<TYPE>-<Mon>-<YYYY>
```
- `<ORG>`: ≤8 chars, no spaces
- `<TYPE>`: `PTI` or `PTE`
- Parallel sub-scopes: append `-01`, `-02`

---

## MRK:PTORC_DIRS — Directory Structure | ptorc,dirs,directory,structure | L73-136

```
<OrgCode>/
  evidence/
    <IP>/                        # one dir per target — IP is primary key
      <tool>_<port>_<TS>.*       # automated tool output
      manual_<desc>_<TS>.txt     # manual session logs (header + pasted output, generated by Claude)
      retest_<TS>/               # re-test evidence
    _sweep/                      # raw scan output only: nmap/db_nmap .nmap/.xml/.gnmap files (03)
    _dns/                        # PTE: DNS/OSINT output (01_dns_recon.sh)
    _ip_analysis/                # PTE: per-IP WHOIS/ASN/BGP/geo/cloud output (02_ip_range_analysis.sh)
    _captures/                   # PTI: pcapng files (Wireshark/tcpdump)
    _msf/                        # MSF spool logs and .rc scripts    _wpscan/                     # WordPress assessment output (06_wpscan.sh)
    _verify/                     # 07_service_verify: per-host probe evidence — one subdir per IP
                                 #   <ip>/ssh_version_*.txt, mysql_noauth_*.txt, ftp_anon_*.txt, etc.
    _exports/                    # MSF DB exports — per phase, all three:
                                 #   hosts_<phase>_<TS>.csv
                                 #   services_<phase>_<TS>.csv
                                 #   notes_<phase>_<TS>.csv  ← NSE script output lives here
                                 #   services_tcp_*.csv      ← written by export_db after phase_tcp;
                                 #                             used as CSV fallback when psql unavailable
  screens/                       # screenshots — flat, named <TS>_<FID>_<IP>_<desc>.png
  scripts/                       # scan/automation scripts + generated target lists
    01_dns_recon_v<N>.sh         # PTE: DNS/OSINT recon → produces targets.txt
    02_ip_range_analysis_v<N>.sh # PTE: passive IP OSINT — ASN, BGP, geo, cloud (no root needed)
    03_comprehensive_scan_v<N>.sh  # PTI+PTE: sweep, enum (--mode pti|pte)
    04_tls_scan_v<N>.sh          # TLS/cert assessment (consumes working/tls_targets.txt)
    05_web_enum_v<N>.sh          # Web enumeration (--tier ghost|normal|loud|evasion)
    06_wpscan_v<N>.sh            # WordPress detection + WPScan assessment (no root needed)
    07_service_verify_v<N>.sh    # Service probe verification — runs after 01–06 complete
    targets.txt                  # PTE: confirmed in-scope IPs — written by 01, read by 02 + 03
    wp_targets.txt               # WordPress URLs from 05 detection — read by 06
  reports/                       # versioned .docx and .md deliverables
  working/                       # analyst working files — NOT raw evidence, NOT deliverables
    scope.md
    <OrgCode>_Findings_vX.X.md   # finding bodies (full text) — fetched only when editing
    open_issues_<YYYYMMDD>.md
    state_<YYYYMMDD_HHMM>_v<N>.md       # STATE SNAPSHOT — primary context file
    target_priority_<YYYYMMDD>.md       # PTI: host priority list from sweep
    dns_summary_<TS>.md                 # PTE: DNS recon summary from 01_dns_recon.sh
    manual_followup_<TS>.md             # script-generated manual follow-up list
    tls_targets.txt                     # HTTPS hosts for 04_tls_scan.sh (written by 03)
    scan_summary_<TS>.md                # per-session scan summary (03)
    tls_summary_<TS>.md                 # TLS assessment summary (04)
    web_summary_<TS>.md                 # web enum summary (05)
    verify_summary_<TS>.md             # service probe results (07) — VULN/SAFE/UNKNOWN/MANUAL tables
```

**Directory rules:**
- `evidence/_sweep/` — raw scan files only (`.nmap`, `.xml`, `.gnmap`, per-session logs)
- `evidence/_dns/` — written by `01_dns_recon.sh`; contains crt.sh, subfinder, amass, httpx, takeover candidates
- `evidence/_ip_analysis/` — written by `02_ip_range_analysis.sh`; per-IP subdirs `<IP_safe>/` (dots→underscores) with WHOIS, BGP, geo, traceroute, Shodan; consolidated `ip_range_report_<TS>.md` at root
- `evidence/_wpscan/` — written by `06_wpscan.sh`; per-target subdirs `<label>/` (URL sanitized) with WPScan output; consolidated `wpscan_report_<TS>.md` at root
- `working/` — all analyst-facing summaries, follow-up lists, target lists, state snapshots
- `evidence/<IP>/` — all per-host tool output and manual session logs
- Manual session logs are generated by Claude from pasted terminal output — never created manually

**MSF DB connection paths (two independent paths):**
- `msfconsole` / `export_db` — Ruby pg adapter, Unix socket; always works on Kali
- `db_query` / `psql` — direct psql; fails if PostgreSQL only listens on Unix socket. `parse_db_conf` tests TCP then Unix socket; sets `DB_DIRECT_AVAILABLE=0` if both fail. Enum phases fall back to `services_tcp_*.csv` already on disk.

---

## MRK:PTORC_STAGES — Stage Registry | ptorc,stages,stage,registry | L137-156

Stages are not strictly linear. A completed stage may be re-entered as a **Return Pass**.

| ID | Stage | PTI | PTE |
|----|-------|-----|-----|
| S0 | Scoping and Setup | ✓ | ✓ |
| S1 | Passive Reconnaissance | limited | full OSINT |
| S2 | Broad Sweep / Surface Mapping | ✓ | ✓ |
| S3 | Service Enumeration | ✓ | ✓ |
| S4 | Vulnerability Assessment | ✓ | ✓ |
| S5 | Exploitation / Validation | ✓ | limited |
| S6 | Evidence Processing | ✓ | ✓ |
| S7 | Report Generation | ✓ | ✓ |
| S8 | Re-test | ✓ | ✓ |

**Stage status values:** `NOT_STARTED` · `IN_PROGRESS` · `COMPLETE` · `COMPLETE+` (complete, with one or more return passes)

---

## MRK:PTORC_DISPATCH — Dispatch Table — L2 Skills | ptorc,dispatch,table,l2,skills | L157-178

L1 reads the request, maps it to an L2, passes the required context, and expects a defined output.

| L2 Skill | Triggers | Pass in from snapshot | Expect back |
|---|---|---|---|
| `pt-recon` | S1/S2 work; "sweep", "OSINT", "DNS", "subdomain", "IP analysis", "surface", new target range | Scope (subnets/domains), tester IPs, MSF workspace, existing host list | Updated host/service inventory, evidence paths, findings stubs |
| `pt-enum` | S3/S4 work; named service ("SMB", "NFS", "SNMP", "TLS", "HTTP", "FTP", "WordPress", specific IP/port), "enumerate", "deeper look" | Target IP, **mode (PTI/PTE)**, **stealth tier**, known open ports/services, existing findings for that host | Evidence paths, findings stubs, validation state updates |
| `pt-evidence` | "process evidence", pasted tool output, "log review", "what does this mean", S6 | Raw output (pasted or file ref), existing findings index | Structured finding stub (ready to append to Findings .md) |
| `pt-report` | "write finding", "draft report", "executive summary", "section", S7 | Findings index, scope, engagement metadata, severity distribution | Finding body text, section text, report-ready content |

### Dispatch Protocol

When a request maps to an L2:
1. State what you are doing: *"Loading pt-enum for SMB enumeration on <target-IP>."*
2. Extract the required fields from the active state snapshot.
3. Invoke the L2 mentally (treat its SKILL.md content as active instructions).
4. On completion, receive L2 output and update the state snapshot.
5. State that L2 work is complete and context is released: *"pt-enum complete for this host. State snapshot updated."*

---

## MRK:PTORC_RETURNPASS — Return Pass Protocol | ptorc,returnpass,return,pass,protocol | L179-196

A return pass occurs when work on an earlier stage is needed after that stage was marked COMPLETE.

**Triggers:** New finding implies re-checking earlier recon data · Client provides additional info · Evidence gap discovered during report writing · Specific host/service needs deeper look after initial pass.

**On return pass initiation:**
1. Identify the target stage (e.g. S3).
2. State: *"Opening Return Pass 1 on Stage 3 — [reason]."*
3. Append to state snapshot Stage Registry: `S3: COMPLETE+ | RP1 | <date> | <reason>`
4. Dispatch appropriate L2 with: (a) existing evidence/findings for relevant hosts, (b) specific objective of this pass, (c) "return pass" flag so L2 doesn't repeat completed work.
5. On completion, append RP summary to state snapshot under that stage's entry.
6. Resume from prior current position.

**Rule:** Return pass does not reset stage status. `COMPLETE+` means complete with additional passes — the `+` count is implicit from RP entries.

---

## MRK:PTORC_STATE — State Snapshot — Schema | ptorc,state,snapshot,schema | L197-420

The state snapshot is the **only artefact needed to reconstruct full engagement context**. It is generated at end of every session and on any significant state change. Loading the snapshot replaces re-reading any skill or evidence file.

Filename: `working/state_<YYYYMMDD_HHMM>_v<N>.md`
Version: increment `v<N>` each save. Never reissue.

````markdown
# State Snapshot — <OrgCode> — v<N>
*<YYYY-MM-DD HH:MM> | Session <N>*

---

## 1. Engagement Metadata

| Field | Value |
|---|---|
| Engagement ID | <OrgCode>-<TYPE>-<Mon>-<YYYY> |
| Client | <Client name> |
| Type | PTI / PTE |
| Tester | <n> |
| Start date | <date> |
| End date (planned) | <date> |
| MSF Workspace | <workspace name> |
| Script suite version | 0.72 |

---

## 2. Stage Registry

| Stage | Status | Notes |
|---|---|---|
| S0 — Scoping | COMPLETE | Scope confirmed <date> |
| S1 — Passive Recon | <status> | <notes> |
| S2 — Broad Sweep | <status> | <notes> |
| S3 — Service Enum | <status> | <notes> |
| S4 — Vuln Assessment | <status> | <notes> |
| S5 — Exploitation | <status> | <notes> |
| S6 — Evidence Processing | <status> | <notes> |
| S7 — Report Generation | <status> | <notes> |
| S8 — Re-test | NOT_STARTED | — |

---

## 3. Scope and Configuration

- **In-scope subnets (PTI):** <CIDR list>
- **In-scope domains/IPs (PTE):** <domain list>
- **Exclusions:** <IPs/subnets excluded>
- **Tester VM IPs (PTI):** <IP list>
- **Domain / NetBIOS (PTI):** <domain.local / NETBIOS>
- **Source IP (PTE):** <VPN exit IP — verified before each active phase>
- **Tier selected:** <ghost|normal|loud|evasion>
- **WordPress targets:** <none detected | see scripts/wp_targets.txt>
- **Cloud-hosted IPs flagged by 02:** <none | list IPs — RoE confirmed Y/N>

---

## 4. Host and Service Inventory
*Source of truth: MSF DB. Discrepancies noted inline.*

| IP | Hostname | OS | Key Ports/Services | Notes | Last Updated |
|---|---|---|---|---|---|
| <IP> | <hostname> | <OS / vendor> | <ports> | <notes or db.caveat> | <date> |

*MSF DB caveats:*
- <IP>: <misidentification noted, raw log is ground truth>

---

## 5. Findings Index
*Full finding bodies in `working/<OrgCode>_Findings_vX.X.md`. Load that file only when editing a specific finding.*

| ID | Title | Sev | Conf | Validation | Status | Evidence Path(s) |
|---|---|---|---|---|---|---|
| F-01 | <Finding title> | High | CERTAIN | high_confidence | Open | evidence/<IP>/<tool>_<TS>.txt |
| F-02 | <Finding title> [Prec.] | Medium | LIKELY | suspected | Open | evidence/_sweep/<tool>_<TS>.txt |
| OBS-01 | <Observation title> | — | CERTAIN | validated | Open | evidence/<IP>/nmap_<TS>.txt |

### Severity Distribution
| Severity | Count |
|---|---|
| High | N |
| Medium | N |
| Low | N |
| Informational | N |
| Observations | N |
| **Total** | **N** |

---

## 6. Open Issues

### Blocking
- [ ] <FID>: <description> — awaiting <what>

### In Progress
- [ ] <topic>: <what is needed>

### Unresolved
- <question needing clarification>

### Resolved (this session)
- [x] <FID>: <description> — <how resolved>

---

## 7. Skills and Scripts Index
*L1 knows these exist. Load skills only when dispatching.*

### L2 Skills
| Skill | Location | Purpose |
|---|---|---|
| pt-recon | /mnt/skills/user/pt-recon/SKILL.md | S1/S2: passive recon, OSINT, sweep |
| pt-enum | /mnt/skills/user/pt-enum/SKILL.md | S3/S4: service enum, vuln assessment |
| pt-evidence | /mnt/skills/user/pt-evidence/SKILL.md | S6: evidence processing, finding stubs |
| pt-report | /mnt/skills/user/pt-report/SKILL.md | S7: report content, our style |
| pt-commands | /mnt/skills/user/pt-commands/SKILL.md | L3: all CLI commands, grouped by service/tech |

### Automation Scripts (scripts/ dir)
**PTI:** `03 --mode pti` → `04` → `05` → `06` (if WordPress) → `07` (service verify)
**PTE:** `01` → `02` → `03 --mode pte` → `04` → `05` → `06` (if WordPress) → `07` (service verify)

| Script | Mode | Runs as | Phase | Key output |
|---|---|---|---|---|
| `01_dns_recon_v<N>.sh` | PTE | root | S1 | `evidence/_dns/`, `scripts/targets.txt`, `working/dns_summary_*.md` |
| `02_ip_range_analysis_v<N>.sh` | PTE | any | S1 (after 01) | `evidence/_ip_analysis/`, `ip_range_report_<TS>.md` — reads `targets.txt`; passive only, no target contact |
| `03_comprehensive_scan_v<N>.sh` | PTI+PTE | root | S2+S3 | `evidence/_sweep/`, `evidence/<IP>/`, `evidence/_exports/`, `working/tls_targets.txt` |
| `04_tls_scan_v<N>.sh` | PTI+PTE | root | S3 | `evidence/<IP>/testssl_*`, `working/tls_summary_*.md` |
| `05_web_enum_v<N>.sh` | PTI+PTE | root | S3 | `evidence/<IP>/` web output, `working/web_summary_*.md`, `scripts/wp_targets.txt` |
| `06_wpscan_v<N>.sh` | PTI+PTE | any | S3 | `evidence/_wpscan/<label>/`, `evidence/_wpscan/wpscan_report_*.md` |
| `07_service_verify_v<N>.sh` | PTI+PTE | root | S4 | `working/verify_summary_*.md`, `evidence/_verify/<ip>/`, `evidence/_msf/verify_*.log` |

### Phase 07 — Service Verify (07_service_verify.sh) [VAPT-Enhanced v1.0]

**Purpose:** Automated service probes against all flagged hosts from prior phases.
Extends findings — does NOT re-run scans from 01–06.

**Run:**
```
sudo ./07_service_verify.sh --mode pte|pti [--severity critical|all]
```

**What it probes (from manual_followup + MSF DB):**

*Original probes:*
- DB NOAUTH: Redis, MySQL, PostgreSQL, MongoDB, MSSQL
- SSH: CVE-2024-6387 regreSSHion version check (range 8.5p1–9.7p1)
- FTP anonymous login
- SMB null session
- SNMP default community string "public"
- SMTP open relay
- Telnet cleartext protocol detection
- IPMI cipher-zero auth bypass
- RDP NLA enforcement check
- WinRM auth methods check
- NFS world-accessible exports
- Web: missing security headers, version disclosure, default pages, CORS
- SSRF→IMDS quick sweep (8 params) — always MANUAL result, needs Burp follow-up

*VAPT-Added probes (2024-2025 CVEs):*
- SSH enhanced: + CVE-2024-6409 (privsep race 8.7p1–9.7p1) + CVE-2023-48795 (Terrapin < 9.6p1)
- SMB enhanced: + CVE-2017-0144 MS17-010 EternalBlue via nmap NSE + signing check
- Web enhanced: + sensitive file exposure (.env, .git, phpinfo, backups) + CVE-2024-4577 PHP CGI
- Elasticsearch/OpenSearch NOAUTH (port 9200) — CWE-306
- Kibana NOAUTH (port 5601) — dashboard credential exposure
- etcd NOAUTH (port 2379) — Kubernetes secret store, CVSS 10.0
- Spring Boot Actuator (port 8080+) — /actuator/env credential leak, /heapdump
- Jenkins: CVE-2024-23897 arbitrary file read (< 2.442) + anonymous access + script console
- CUPS/IPP: CVE-2024-47076/47175/47176/47177 exposure (port 631) — RCE chain

**New auto-detected ports (MSF DB sweep):** 631, 2379, 5601, 8200, 8500, 9200

**CVE Reference Table:**
| CVE | Service | Severity | Description |
|-----|---------|----------|-------------|
| CVE-2024-6387 | OpenSSH 8.5p1–9.7p1 | CRITICAL | regreSSHion — SIGALRM signal handler race → unauthenticated RCE |
| CVE-2024-6409 | OpenSSH 8.7p1–9.7p1 | HIGH | privsep child race condition — lower-severity sibling of 6387 |
| CVE-2023-48795 | OpenSSH < 9.6p1 | MEDIUM | Terrapin attack — SSH prefix truncation, MitM downgrade |
| CVE-2024-23897 | Jenkins < 2.442 | CRITICAL | Arbitrary file read via CLI @-file → RCE escalation |
| CVE-2024-47076 | libcupsfilters ≤ 2.1b1 | CRITICAL | Unsanitized IPP attributes → malicious PPD injection |
| CVE-2024-47176 | cups-browsed ≤ 2.0.1 | CRITICAL | Binds UDP 0.0.0.0:631, accepts any Get-Printer-Attributes |
| CVE-2024-4577 | PHP CGI (Windows) | CRITICAL | PHP-CGI argument injection bypassing CVE-2012-1823 fix |
| CVE-2017-0144 | Windows SMBv1 | CRITICAL | MS17-010 EternalBlue — remote code execution via SMBv1 |
| CWE-306 | ES/etcd/Kibana | CRITICAL | Missing authentication for critical function (no-auth services) |
| CWE-200 | Spring Actuator | HIGH | Information exposure via unsecured management endpoints |

**Output:** `working/verify_summary_TIMESTAMP.md`
- Sections: VULN (confirmed) | SAFE | UNKNOWN | MANUAL
- Format: markdown table per section: Host | Port | Service | Result | Evidence

**Evidence:** `evidence/_verify/<ip>/` — one file per probe type per host
- `ssh_version_*.txt`, `mysql_noauth_*.txt`, `ftp_anon_*.txt`, etc.
- `elasticsearch_noauth_*.txt`, `etcd_noauth_*.txt`, `jenkins_*.txt` (VAPT-added)
- `evidence/_msf/verify_<module>_<ip>_*.log` — MSF spool files

**When to load verify_summary:**
When user asks about service-level findings, DB access, SSH versions, or
requests finding stubs — verify_summary is the PRIMARY source alongside
manual_followup. Load it before generating stubs.

**Key findings this engagement (WaterTech):**
- PTE: MySQL NOAUTH (35.214.247.76:3306), SSH CVE (3 hosts), web headers (13 hosts)
- PTI: PostgreSQL NOAUTH (×2), MSSQL SA empty pw (×2), FTP anon (×5), SSH CVE (×9)

---

## 8. Current Position

| Field | Value |
|---|---|
| Active stage | S<N> — <Stage name> |
| Last action | <what was done last session> |
| Next action | <exactly what to do next session> |
| Blocking | <FID or issue — or "none"> |
| Report version | Findings v<N> in progress / not started |
| Last snapshot | state_<prev_TS>_v<N-1>.md |

---
*pt-orc v0.72 | State snapshot schema v0.72*
````

---

## MRK:PTORC_SESSIONSTART — Session Start Protocol | ptorc,sessionstart,session,start,protocol | L421-427

1. User provides state snapshot (paste or file reference).
2. L1 reads snapshot → reconstructs: metadata, stage status, findings index, open issues, current position.
3. Confirm reconstruction: *"Context loaded. Active stage: S3. Next action: [X]. Blocking: [Y]. Ready."*
4. Do not load any L2 until a specific request maps to one.

## MRK:PTORC_SESSIONEND — Session End Protocol | ptorc,sessionend,session,end,protocol | L428-436

1. Collect all changes from session: new/updated findings, stage status changes, resolved issues, new hosts.
2. Generate updated state snapshot (increment version).
3. List what changed since last snapshot.
4. Remind of any blocking items before closing.

---

## MRK:PTORC_CHECKLISTS — Checklists | ptorc,checklists | L437-477

### Before First Active Packet
- [ ] Written authorisation confirmed and on file
- [ ] `scope.md` complete (targets, exclusions, RoE, emergency contact)
- [ ] PTE: `01` run — `scripts/targets.txt` populated; DNS summary reviewed
- [ ] PTE: `02` run — cloud-hosted IPs flagged; RoE confirmed for each
- [ ] PTE: tester VPN exit IP verified (`TESTER_IP` set in 01 + 03; `verify_source_ip()` aborts on mismatch)
- [ ] PTE: confirm outbound IP matches whitelisted IP before each active phase
- [ ] MSF workspace created, active, verified (`msf6 > workspace`)
- [ ] Evidence directory tree created (including `_dns/`, `_ip_analysis/`, `_wpscan/`)
- [ ] Spool log enabled; file confirmed on disk

### End of Each Session (before generating snapshot)
- [ ] All tool output saved to `evidence/<IP>/` or appropriate `evidence/_*/` dir
- [ ] Screenshots named with IP + FID in `screens/`
- [ ] Manual notes written with standard header
- [ ] MSF DB exported — hosts + services + **notes** CSVs to `evidence/_exports/`
- [ ] MSF DB caveats noted for misidentified hosts/services
- [ ] `open_issues` updated
- [ ] No test artefacts left on client systems
- [ ] **State snapshot generated and saved**

### Before Report Draft
- [ ] All three evidence streams populated per finding
- [ ] No High at `detected` state only
- [ ] Precautionary findings labelled `[Precautionary]`, confidence LIKELY
- [ ] Every F-XX has a Recommendation; no OBS-XX has one
- [ ] Severity distribution table verified

### Before Report Release
- [ ] Version incremented in filename AND internal field
- [ ] Executive Summary written last
- [ ] Severity distribution verified against findings index
- [ ] PTE: categories in correct sequence
- [ ] PTI: category count ≤ 8
- [ ] No references to prior versions in body text
- [ ] Appendix — Evidence Index populated

---

## MRK:PTORC_PITFALLS — Common Pitfalls | ptorc,pitfalls,common | L478-506

| Pitfall | Correct approach |
|---|---|
| Evidence not linked to target IP | Every file in `evidence/` under `<IP>/` or named `_` dir |
| MSF DB accepted as ground truth | Flag misidentifications; raw log wins on conflict |
| High from single stream | Require two streams or active confirmation |
| Precautionary CVE without label | Add `[Precautionary]`; set confidence LIKELY |
| Finding downgraded without rationale | Add explicit note to finding body |
| Subnet bridge from one indicator | Require sequential MACs + identical interface stats |
| Session ended without snapshot | Generate snapshot before closing — always |
| Same version number reissued | Increment before saving; filename must match internal field |
| Finding without Recommendation | Every F-XX requires Recommendations section |
| Recommendation on Observation | OBS-XX informational only; promote to F-XX if action needed |
| Workspace not verified at session start | `msf6 > workspace` before any command |
| Return pass wiping prior progress | COMPLETE+ preserves history; RP appends, never resets |
| 02 run after 03 starts scanning | 02 is passive OSINT only — must run before 03 sends any packet |
| 06 run without wp_targets.txt | 05 populates it; or use `06 --detect` to run detection sweep first |
| `--yes` used for PTE scan | Never skip PTE scope confirmation; visual confirm required every run |
| WordPress finding without 06 evidence | Run 06 after 05 to get structured WPScan output before writing findings |
| "No hosts in DB" during enum/udp | Check DB_DIRECT_AVAILABLE — psql may be failing (Unix socket vs TCP). CSV fallback activates automatically; enum proceeds from services_tcp_*.csv |

---
*pt-orc SKILL.md v0.73-vapt — L1 Orchestrator [VAPT-Enhanced 2026-06-05]*
*Replaces: pt-workflow v6.1 | Script suite: v0.73-vapt*
*VAPT enhancements: 2024-2025 CVE probes, Elasticsearch/etcd/Jenkins/CUPS coverage*

<!-- L2 NAV:v1 → ../../../AUDIT-ORC-INDEX.md -->
<!-- L1 ORC-NAV — read MRK:NAV_TOC first; fetch MRK ranges precisely (no default line count) -->
